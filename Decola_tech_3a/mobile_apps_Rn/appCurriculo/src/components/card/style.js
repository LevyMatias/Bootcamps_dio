import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    card: {
        width: '60%',
        borderWidth: 1,
        borderColor: '#4ae84a',
        paddingVertical: 5,
        alignItems: 'center',
        backgroundColor: '#fff'
      },
      card_content: {
        alignItems: 'center',
      },
      title_card: {
        fontWeight: 'bold',
        fontSize: 20,
        marginBottom: 10,
      }
});
export default style;